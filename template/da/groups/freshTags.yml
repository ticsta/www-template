###
# select fresh tags
###

source: db
params:
  action: list
  model: topgroupitem
  select:
    - fields
  where:
    groupName: tags
    selector: '@{selector|.}'
    updatedAt:
      $gte: '@{time|$1h}'
  order:
    updatedAt: DESC
  limit: '@{limit|10}'
  offset: '@{offset|0}'