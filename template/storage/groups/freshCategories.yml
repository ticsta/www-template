###
# select fresh categories
###

source: db
params:
  action: list
  model: topgroupitem
  select:
    - fields
  where:
    groupName: categories
    selector: '@{selector|.}'
    updatedAt:
      $gte: '@{time|$1h}'
  order:
    updatedAt: DESC
  limit: '@{limit|10}'
  offset: '@{offset|0}'